version: "3.8"
services:
  labomen-bot-dev:
    build: ./
    tty: true
    working_dir: /opt/labomen-bot/
    container_name: labomen-bot-dev
    volumes:
      - ..:/opt/labomen-bot:delegated
      - ~/.ssh:/root/.ssh:delegated
